{
    "name": "example",
    "version": "0.0.1",
    "main": "index.js",
    "type": "module",
    "scripts": {
        "build": "rm -rf ./dist/* && tsc --outDir dist/ ",
        "start": "node --loader ts-node/esm index.ts"
    },
    "dependencies": {
        "@glacier-network/langchain-glacierdb": "^0.0.1"
    },
    "devDependencies": {
        "ts-node": "^10.9.2"
    }
}
